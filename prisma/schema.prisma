generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../db_store/dev_tld_domain_names.db"
}

// introspection infers models from the db.
model domain_names_table {
  id          String  @unique
  domain_name String?
}
