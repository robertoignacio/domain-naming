at the root of the project:

$ python3 -m venv venvname

activate venv:
$ source venvname/bin/activate

exit venv for dealing with other packages
$ deactivate


-----------
venv installed packages:
$ pip install tqdm
$ pip install requests
$ pip install beautifulsoup4