# do not push db files to github because they tend to become large
# also, this python project at the moment generates sqlite db files, which will consume github storage
# use the janitor_db_vacuum.py to clean up the db file of unused space
db_store/*

# Ignore the large real DNS registry files
/inputfiles/icann_tld_dev_registered_domains_latest.txt

# do not include the venv directory
venvname/

# control_vars.py stores variable values as cache
__pycache__

# ignore the node_modules directory
node_modules

# ignore the .next directory
.next